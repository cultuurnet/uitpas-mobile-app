import React from 'react';
import { ScrollView, Text } from 'react-native';

const Home = () => {
  return (
    <ScrollView>
      <Text>This is an example view</Text>
    </ScrollView>
  );
};

export default Home;
