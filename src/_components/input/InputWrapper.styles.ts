import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    marginBottom: 18,
  },
  disabled: {
    opacity: 0.65,
  },
  icon: {
    marginBottom: 2,
    marginRight: 6,
  },
  labelContainer: {
    alignItems: 'center',
    flexDirection: 'row',
    marginBottom: 4,
  },
});
