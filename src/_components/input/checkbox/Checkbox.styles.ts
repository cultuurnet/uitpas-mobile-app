import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  checkbox: {
    alignItems: 'center',
    display: 'flex',
    flexDirection: 'row',
    paddingVertical: 8,
  },
  icon: {
    marginRight: 8,
  },
});
