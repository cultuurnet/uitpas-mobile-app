import React from 'react';
import { ScrollView, Text } from 'react-native';

const Scan = () => {
  return (
    <ScrollView>
      <Text>This is the scan page</Text>
    </ScrollView>
  );
};

export default Scan;
